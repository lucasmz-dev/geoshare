<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.GeoShare" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/ic_launcher_background
        </item>
    </style>

    <!--
    This theme is a combination of android:Theme.Material.Light.NoActionBar and
    android:Theme.Translucent.NoTitleBar, because for some reason
    android:Theme.Translucent.NoTitleBar shows status and navigation bars with
    black background.
    -->
    <style name="Theme.Geoshare.Translucent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
